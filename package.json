{
  "name": "imprison",
  "version": "0.0.8",
  "description": "wrap files inside an immediately invoked function and specify calling context",
  "main": "index.js",
  "bin": {
    "imprison": "./cli.js"
  },
  "scripts": {
    "prepublish": "npm run build",
    "build": "rollup -c rollup.config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "talmobi <talmo.christian@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "minimist": "1.2.0"
  },
  "devDependencies": {
    "rollup": "^0.41.4",
    "rollup-plugin-buble": "^0.15.0"
  }
}
